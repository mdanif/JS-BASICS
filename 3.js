// let is used when we need to reassign 
let interestRate = 0.3;
interestRate = 1;
console.log(interestRate);

// You can't reassign while using const
const rateInsterest = 0.3;
rateInsterest = 1;
console.log(rateInsterest);