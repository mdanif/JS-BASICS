
let selectedColors = ['red', 'blue']; // Technicaly an array is object in JavaScript
selectedColors[2] = 1;
console.log(selectedColors.length);

// An array is a data structure that we ues to repersent a list of items.
