// Object
let person = {
    name: 'irshad',
    age: 22
};

// Dot Notatation
person.name = 'imran';

// Bracket Notation
let selection = 'name';
person[selection] = 'ali';

console.log(person.name);