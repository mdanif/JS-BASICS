// Strict equality (Checks: Type + Value)
console.log (1 === 1);
console.log('1' === 1);

// Lose equality (Checks: Value)
console.log(1 == 1);
console.log('1' == 1); // converts right side value to a string because values are same.

console.log(true == 1); // it convert right side one.